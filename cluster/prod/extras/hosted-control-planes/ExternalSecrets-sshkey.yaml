---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: external-secrets-hcp-sshkey-prod
  namespace: clusters
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: sshkey-cluster-prod
  data:
    - secretKey: id_rsa.pub
      remoteRef:
        key: cluster/prod/install/ssh
        property: public_key
