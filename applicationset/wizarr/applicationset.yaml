apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gitops-wizarr
  namespace: openshift-gitops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: environment
              operator: In
              values:
                - production
  template:
    metadata:
      name: '{{ .name }}-wizarr'
    spec:
      project: '{{ .name }}'

      destination:
        server: '{{ .server }}'
        namespace: arr-management

      sources:
        - repoURL: https://gitlab.developer.hkn.lab/IaC/openshift4/clusters/all.git
          targetRevision: HEAD
          path: clusters/{{.name}}/extras/wizarr
          ref: values

        - repoURL: https://charts.hknlab.io
          chart: wizarr
          targetRevision: 0.1.5
          helm:
            releaseName: wizarr
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/clusters/{{.name}}/values/wizarr.yaml

      syncPolicy:
        automated:
          prune: false
        syncOptions:
        - CreateNamespace=true
