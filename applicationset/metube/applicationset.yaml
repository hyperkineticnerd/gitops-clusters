apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gitops-metube
  namespace: openshift-gitops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: environment
              operator: In
              values:
                - production
  ignoreApplicationDifferences:
    - jsonPointers:
        - /spec/syncPolicy
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      name: '{{ .name }}-metube'
    spec:
      project: '{{ .name }}'

      destination:
        server: '{{ .server }}'
        namespace: metube

      sources:
        - repoURL: https://charts.hknlab.io
          chart: metube
          targetRevision: 0.1.0
          helm:
            releaseName: metube
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/cluster/{{.name}}/values/metube.yaml

        - repoURL: https://gitlab.developer.hkn.lab/IaC/openshift4/clusters/all.git
          targetRevision: HEAD
          path: cluster/{{.name}}/extras/metube
          ref: values

      syncPolicy:
        automated:
          enabled: false
          prune: false
          selfHeal: false
        syncOptions:
        - CreateNamespace=true

      # ignoreDifferences:
      #   - group: Batch
      #     kind: Job
