apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gitops-matrix-redis
  namespace: openshift-gitops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: environment
              operator: In
              values:
                - production
  ignoreApplicationDifferences:
    - jsonPointers:
        - /spec/syncPolicy
  syncPolicy:
    preserveResourcesOnDeletion: false
  template:
    metadata:
      name: '{{ .name }}-matrix-redis'
    spec:
      project: '{{ .name }}'

      destination:
        server: '{{ .server }}'
        namespace: matrix-synapse

      sources:
        - repoURL: https://gitlab.developer.hkn.lab/IaC/openshift4/clusters/all.git
          targetRevision: HEAD
          path: cluster/{{ .name }}/extras/matrix-redis
          ref: values

        - repoURL: registry-1.docker.io/bitnamicharts
          chart: redis
          targetRevision: 27.0.13
          helm:
            releaseName: redis
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/cluster/{{ .name }}/values/matrix-redis.yaml

      syncPolicy:
        automated:
          enabled: false
          prune: false
          selfHeal: false
        syncOptions:
        - CreateNamespace=true
